import React from "react";
import ReactDOM from "react-dom/client";

function App() {
  return <main style={ padding: 24 }><h1>AIOS Status Panel</h1></main>;
}

ReactDOM.createRoot(document.getElementById("root")!).render(<App />);
