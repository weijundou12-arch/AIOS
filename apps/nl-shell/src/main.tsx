import React from "react";
import ReactDOM from "react-dom/client";

function App() {
  return <main style={ padding: 24 }><h1>AIOS NL Shell</h1></main>;
}

ReactDOM.createRoot(document.getElementById("root")!).render(<App />);
